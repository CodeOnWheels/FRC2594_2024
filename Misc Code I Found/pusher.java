package frc.robot.shooterAndIntake;

import edu.wpi.first.wpilibj.motorcontrol.PWMSparkMax;


public class pusher{

    // instantiates sparkmax obj
    // PWMSparkMax pusherMotor1 = new PWMSparkMax(3);

    //sets speed
    // double motorSpeed = 0.5;

    // motor would turn clockwise
    public void Forward(){
        // pusherMotor1.set(motorSpeed);
    }

    // motor turns counter clockwise
    public void Reverse(){
        // pusherMotor1.set(-motorSpeed);
    
    }

    // motor stops
    public void Stop(){
        // pusherMotor1.set(0);
    }
}